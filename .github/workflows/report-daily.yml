name: IPC Fans 1-Day Report

on:
  workflow_dispatch:
  schedule:
    - cron: '17 7 * * *'
      timezone: Asia/Taipei

permissions:
  contents: read

concurrency:
  group: ipc-fans-1-day-report
  cancel-in-progress: true

jobs:
  generate:
    uses: ./.github/workflows/report-reusable.yml
    with:
      cadence: daily
      report-title: IPC / Edge AI 1-Day 區間新聞 Email
      news-days: 1
      per-company-limit: 2
      artifact-name: report-daily
      artifact-retention-days: 14
    secrets:
      gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
